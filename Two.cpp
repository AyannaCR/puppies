int sumTriangularNumbers(int n)       //n represents the inital value	
{
	int sum;
	sum = 1; 
	for (int i = 1; i < n; ++i)      //loop  while i (the first level) is less than n
	{

	}
}

/*int triangular_numbers(int n) {
  int d = 1,j = 2;
  for(int i = 1; i < n; i++)d+=j++;
  return d;
}
